package gs.util.bindings 
{
	
	/**
	 * The BindableObject class is a base object
	 * you can use directly, or extend, to enable
	 * bindings on properties it contains.
	 * 
	 * <script src="http://mint.codeendeavor.com/?js" type="text/javascript"></script>
	 */
	public class BindableObject extends ObjectProxy 
	{
		
		/**
		 * Constructor for BindableObject instances.
		 */
		public function BindableObject()
		{
			super();
		}
	}
}